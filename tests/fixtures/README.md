# Test fixtures

Run `python scripts/make_sample_pdf.py` from the repo root to generate sample PDFs here for manual QA.

Generated files (gitignored if large; script recreates them):

- `sample_form.pdf` — 3-page repeating form with phone/email
- `sample_phone.pdf` — single page with a phone number
